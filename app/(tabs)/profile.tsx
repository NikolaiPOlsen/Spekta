/**
 * Renders the profile route inside the main tab navigator.
 */

import { ProfileScreen } from '@/features/profile/components';

export default function ProfileRoute() {
  return <ProfileScreen />;
}
