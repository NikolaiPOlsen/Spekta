/**
 * Renders the recommendations route inside the main tab navigator.
 */

import { RecommendationsScreen } from '@/features/recommendations/components';

export default function RecommendationsRoute() {
  return <RecommendationsScreen />;
}
